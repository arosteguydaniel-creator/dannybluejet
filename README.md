# Danny Blue Jet

Sitio Next.js (branch feature/nextjs-site). Para ejecutar localmente:

1. git fetch origin
2. git checkout feature/nextjs-site
3. npm install
4. npm run dev

Reemplaza los tokens de Flow en app/api/productos/route.ts y sube tus MP3 si los tienes.
